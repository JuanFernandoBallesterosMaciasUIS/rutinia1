# Django Configuration
# Genera tu propia SECRET_KEY con: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=tu-secret-key-aqui
DEBUG=False

# Hosts permitidos (separados por coma, sin espacios)
# En Railway, añade: tu-app.up.railway.app
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS - Orígenes permitidos (separados por coma, sin espacios)
# Añade tu frontend de Vercel: https://tu-app.vercel.app
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Database (MongoDB)
MONGO_DB=tracker_habitos_db
DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority&appName=YourCluster
